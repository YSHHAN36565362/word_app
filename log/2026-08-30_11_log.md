# 2026-08-30 — 프론트 마무리 작업 여러 건

## 요청 (요약)
- 각 파트(학습/연습/매칭게임/시험/오답노트/설정)에 넣을 일러스트 텍스트 설명 요청.
- 하단 탭 버튼이 너무 작아서 있는지 모르는 사람이 있다 — 크기 키우고, 순서대로
  움직이는 애니메이션 추가.
- N2 폴더의 8개 날짜별 Tr.Park 단어장을 `N2(Tr.Park_word)` 폴더로 복사(날짜 제거),
  선택을 더 쉽게.
- 연습/시험의 "이름만/뜻만/랜덤" 버튼 이름이 헷갈린다 — 더 직관적으로. `_IT`는
  일본어가 아니니 기존 이름 유지.
- 연습 파트에서 채점 후 나오는 일러스트를 난이도별(모름/헷갈림/조금앎/완벽함)로
  다양화하고 싶다 — 텍스트로 아이디어 요청.
- 안키처럼, 모름/헷갈림이면 다음 날 위쪽 스택으로, 조금 앎은 3일 후, 완벽함은 7일
  후로 옮겨주는 기능 구현 요청 (내 번호 저장했을 때만).
- N2 폴더 단어 수(약 1,753개)가 하루 학습량으로 너무 많다 — 개선 방향 제안 요청.
- 오늘/이전 요청·작업 내역을 `log` 폴더에 날짜별로 정리해달라는 요청(이 로그).
- (중간 추가 요청들) 로컬 개발 서버를 크롬에 띄워 같이 확인, `N2(Tr.Park_word)`를
  누르면 폴더 전체 자동 체크 + 목록 맨 위 고정, 학습 파트 힌트 버튼 강조 + Space
  단축키 추가, 매칭 게임 쉬운/어려운 모드 분리.
- README가 너무 방대해질 것 같으니 날짜별로 파일을 쪼개자는 요청(이 구조 변경).
- 연습을 라운드(15개) 단위로: 모름은 그 라운드 안에서 무조건 다시, 헷갈림은
  2번째 라운드나 3~5번째 라운드 중 하나로 미루기.
- "연습 파트" 옆에 "연습 복습" 파트 신설: 모름으로 5회 이상 채점된 단어만 모아
  한자→탭하면 뜻이 보이는 아주 가벼운 화면으로.
- 단어 순서 섞기에 확인 문구 추가, 정말 랜덤인지 재확인.
- (고민해달라고 남겨둔 것) 라운드가 정해지면 새 단어는 그 라운드가 끝나야(15개를
  다 클리어해야) 나오게 하는 안.

## 한 일

### UI/네비게이션
- **하단 탭 내비게이션**: 아이콘을 28px→36px 원으로 키우고, 터치 영역도 넓힘.
  탭이 하나씩 순서대로 통통 튀는 웨이브 애니메이션을 계속 무한 반복(짧게 튀고
  길게 쉬었다가 처음부터)해서 기능이 있다는 걸 알아채기 쉽게 함.
- **연습/시험 모드 버튼 이름**: 일본어 단어장에서 "이름만"→"한자 맞추기",
  "뜻만"→"한자 뜻 맞추기"로 변경. 선택한 파일이 전부 `_IT` 카테고리일 때만 기존
  이름("이름만"/"뜻만") 유지 — `FileRef.path`에 `/_IT/`가 포함돼 있는지로 판별
  (`isItSelection` 헬퍼를 `lib/types.ts`에 추가).
- **학습 파트 힌트 버튼**: 눈에 안 띄던 회색(ghost) 버튼을 amber색으로 바꾸고,
  새 단어 카드가 나올 때마다(카드당 1회, 계속 반복 아님) 살짝 튀어 오르는 애니메이션
  추가. 단축키에 Space를 추가 — H는 한글/일본어 자판일 때 `e.key`가 다르게 잡혀
  안 먹혔던 문제라, 자판과 무관한 물리 키 코드(`KeyH`)와 Space를 함께 등록해서
  해결.
- **매칭 게임 난이도 분리**: 기존 "매칭 게임 시작"을 "쉬운 매칭 게임 시작"으로
  바꾸고, "어려운 매칭 게임 시작"을 새로 추가. 어려운 모드는 뜻 옆 괄호 안
  요미가나(예: "조립하다(くみたてる)")를 정규식으로 지워서 표시 — 괄호 속 가나만
  보고 짝 맞히는 편법을 막음. 재시작("같은 조합으로 다시하기")도 방금 골랐던
  난이도를 그대로 유지.

### 단어장/파일 선택
- **`N2(Tr.Park_word)` 폴더 신설**: 기존 8개 날짜별 파일(2026-08-13~08-26)을 원본은
  그대로 두고 복사, 파일명에서 날짜 접두어만 제거.
- **FileSelector 기본값 변경**: 카테고리 진입 시 기본으로는 N2·N3 세부 카테고리만
  체크되고, 나머지(N1, N2(Tr.Park_word) 등)는 사용자가 직접 눌러야 켜지도록 변경.
  체크된 세부 카테고리 pill 안에 초록색 체크마크(✓) 배지 추가.
- **`N2(Tr.Park_word)` 특별 취급**: 이 폴더를 누르면 안의 파일 전체가 즉시 체크됨
  (해제 시 전체 해제)。 목록에서도 "날짜 없음"에 섞여 묻히지 않도록 별도의
  "모아보기 (고정)" 그룹으로 분리해 항상 맨 위(가장 최근 월보다도 위)에 오도록
  정렬. 선택 해제하면 다시 "날짜 없음" 그룹으로 돌아감.
- **단어 순서 섞기 확인**: 이제 큐 순서 자체가 "모름은 이 라운드 안에, 헷갈림은
  몇 라운드 뒤에"라는 의미를 담고 있어서, 섞으면 그 스택이 흐트러진다 — 누르면
  "단어를 섞으시겠습니까?" 확인을 한 번 받도록 `window.confirm` 추가(기존 설정
  화면의 삭제 확인과 같은 패턴). 실제 셔플 함수(`shuffle`)는 원래부터 표준
  Fisher–Yates라 편향 없이 진짜 무작위임을 재확인함(버그 아니었음).

### 간격 반복(SRS) 재설계
- 이번 라운드 초반엔 "모름·헷갈림도 다음 날 위쪽 스택으로" 안을 그대로 구현했는데,
  "내일까지 미루면 오늘 안에 다시 볼 기회가 없어져 학습에 방해된다"는 피드백을
  받고 즉시(간격 0) 방식으로 되돌림. **최종적으로**:
  - 조금 앎(60) → 3일 후, 완벽함(100) → 7일 후 날짜 기준으로 다시 큐 위쪽에 노출
    (`FIXED_INTERVAL_DAYS`를 예전의 SM-2식 점점 늘어나는 간격 대신 점수별 고정값으로
    단순화).
  - 모름(0)·헷갈림(40)은 간격 0(항상 "지금 다시 봐야 함")을 유지하되, 실제로 몇 턴
    뒤에 다시 나오는지는 **세션 내 라운드 기준**으로 재설계함(아래 항목).
  - 채점 버튼에 "3일 후"/"7일 후"/"곧 다시"/"잠시 후 다시" 같은 간격 힌트 라벨 추가.
- **라운드 기반 재출제(`queue.ts`의 `requeuePosition` 재설계)**:
  - `ROUND_SIZE`(15)를 `practice/page.tsx`에서 `queue.ts`로 옮겨 두 로직이 같은
    값을 공유하도록 함.
  - 모름(0): ROUND_SIZE 턴 이내(=이번 라운드 안)의 무작위 위치에 반드시 재삽입.
  - 헷�갈림(40): 50% 확률로 "다음 라운드"(ROUND_SIZE~2×ROUND_SIZE-1번째 위치) 또는
    "3~5번째 라운드"(2×ROUND_SIZE~5×ROUND_SIZE-1번째 위치) 중 하나를 골라 그 구간
    안의 무작위 위치에 재삽입.
  - 예전의 "남은 큐의 20~40%/5~15% 지점" 방식(큐 길이에 비례)에서 "몇 번째 라운드"
    기준(라운드 크기에 비례)으로 바뀐 것 — 큐가 아주 길거나 짧아도 "몇 라운드 뒤에
    다시 만날지"가 일정하게 유지됨.

### 연습 복습 (신규, 가볍게)
- "사이트가 무거워지는 것 같다"는 우려에 맞춰 **애니메이션·마스코트·SRS 상호작용
  없이** 아주 단순한 목록 화면으로 구현.
- `word_mastery`에 `unknown_count`(모름으로만 채점된 누적 횟수, `wrong_count`와
  달리 헷갈림은 안 셈) 컬럼을 추가(`supabase/schema.sql`, 기존과 같은 additive
  방식이라 기존 데이터에 영향 없음. **Supabase에 이 컬럼을 실제로 적용하려면
  schema.sql을 다시 실행해야 합니다** — 안 하면 이 기능만 조용히 동작 안 함).
- `loadFrequentlyUnknownWords(userId, threshold=5)`: `unknown_count >= 5`인 단어만
  가져옴.
- 새 페이지 `/practice/frequent`: 한자(단어)만 먼저 보이고, 그 줄을 누르면 뜻이
  펼쳐지는 아주 단순한 목록(기존 오답노트 화면과 같은 톤). 채점/삭제/애니메이션
  없음.
- 연습 파트 헤더 오른쪽에 "연습 복습" 버튼 추가(내 번호가 있을 때만 노출).

## 검증
- 매번 `eslint`(`--max-warnings=9999`), `tsc --noEmit`, `next build` 통과 확인.
- GitHub API rate limit으로 인해 브라우저 실사용 검증은 일부 보류(사용자 요청에
  따라 스킵하고 코드/로컬 작업 우선 진행) — 전체 작업이 끝난 뒤 다시 시도 예정.

## 파트별 PageHeader 일러스트 아이디어 (텍스트, 이미지는 사용자가 직접 첨부 예정)

각 파트 헤더의 색 배지(지금은 "학"/"연"/"매"/"시"/"오"/"설" 같은 한 글자) 자리에 넣을
작은 일러스트 아이디어입니다. 배지가 44px 안팎으로 작게 표시되므로, 배경이 단순하고
피사체가 하나뿐인 그림이 좋습니다.

- **학습**: 책상에 앉아 책을 펼쳐 읽고 있는 학생 한 명. 연필을 들고 있어도 좋음 —
  "차분히 훑어본다"는 느낌.
- **연습**: 연필 한 자루 아이콘, 또는 손으로 무언가를 반복해서 쓰는 모습 — "반복
  연습"의 느낌을 주는 단순한 사물/동작.
- **매칭게임**: 퍼즐 조각 두 개가 딱 맞춰지는 모습(직소 퍼즐 아이콘) — "짝을 맞춘다"는
  의미가 바로 전달됨.
- **시험**: 줄이 그어진 답안지/시험지 한 장 아이콘(사람 없이 종이만) — 채점 표시(빨간
  동그라미 등)가 살짝 보이면 더 좋음.
- **오답노트**: 큰 × (엑스) 표시 아이콘, 또는 노트에 빨간 ×가 그려진 모습.
- **설정**: 톱니바퀴(기어) 아이콘 — 겹쳐진 기어 2개도 좋음.

## 난이도별(모름·헷갈림·조금 앎·완벽함) 반응 일러스트 아이디어

지금은 정답(조금 앎+완벽함)/오답(모름+헷갈림) 두 그룹으로만 나뉘어 있는데, 4단계
각각 다른 느낌의 일러스트를 쓰고 싶다는 요청에 따른 텍스트 설명입니다. 이미지는
irasutoya.com 같은 무료 사이트에서 찾아 첨부하면 됩니다.

- **모름 (0) — 가장 절망적/코믹한 반응**: 충격받아 얼어붙은 표정, 넋 놓고 하늘을
  보는 모습, 무릎 꿇고 좌절하는 모습, 진땀을 뻘뻘 흘리는 모습처럼 "완전히 못 맞혔다"는
  걸 과장되게 보여주는 그림. (기존에 넣어둔 해고당한 회사원, 백기 든 사람, 벼 베는
  농부 같은 유머러스한 그림들이 이 칸에 잘 어울림.)
- **헷갈림 (40) — 애매하고 갸우뚱한 반응**: 고개를 갸우뚱하는 모습, 머리 위에 물음표가
  여러 개 떠 있는 모습, 안경을 고쳐 쓰며 고민하는 모습, 양손을 저울처럼 들고 고민하는
  모습 — "확실친 않지만 뭔가 걸린다"는 느낌.
- **조금 앎 (60) — 안도 섞인 반응**: 이마의 땀을 손등으로 닦는 모습, "휴~" 하고 가슴을
  쓸어내리는 모습, 반신반의하며 손가락으로 브이(V)를 하는 모습 — "아슬아슬하게
  맞혔다"는 안도감.
- **완벽함 (100) — 자신감 넘치는 반응**: 트로피를 들어 올리는 모습, 팔짱을 끼고
  당당하게 웃는 모습, 폭죽이 터지는 배경 앞에서 승리 포즈를 취하는 모습 — 기존의 주먹
  불끈 쥐는 이미지와 같은 계열로, 확신에 찬 승리감을 주는 그림.

## `N2` 단어 수(약 1,753개)가 하루 학습량으로 너무 많다는 지적에 대한 제안

구조적으로 "전체 폴더를 한 번에 다 풀어야 하는" 강제가 없다는 점을 먼저 짚고
싶습니다 — 이미 (1) 연습은 15개 단위 라운드로 쪼개져 있고, (2) 파일 선택 화면에서
원하는 날짜/묶음만 골라 학습할 수 있고, (3) SRS가 이미 아는 단어는 자동으로 뒤로
미뤄서 매번 전체를 다시 보지 않아도 됩니다. 다만 "오늘 이 정도만 하면 된다"는 걸
더 명확히 안내해주는 기능(예: 새 단어+오늘 복습 대상만 자동으로 30~50개 뽑아주는
"오늘의 학습 추천" 카드)은 아직 없습니다 — 원하시면 다음에 이어서 만들 수 있습니다.

## 라운드=고정 15단어 잠금 vs. 라운드=15개 맞히기 (결론)

"라운드가 끝나야 새 단어가 나오게" 안(문자 그대로 "모름 15개를 다 클리어하기 전엔
새 단어 절대 금지")을 검토하다가, 라운드가 딱 1개 단어만 남을 정도로 좁혀지면 그
하나를 바로 직전에 본 직후 또 보게 되어 "암기 확인"이 아니라 "방금 본 걸 그대로
재현"이 되어버리는 문제를 발견했습니다. 이 지점에서 "라운드별로 묶는 것 자체가
잘못된 게 아닐까" / "모르는 단어가 5개 이하로 줄면 새 단어를 넣는 방식은 어떨까"
하는 논의가 있었습니다.

**결론: 이미 구현된 방식이 이 문제를 원천적으로 피해가고 있어서 추가 잠금은
넣지 않기로 했습니다.** 지금 큐 구조는 선택한 단어 전체(수십~수백 개)가 처음부터
한 큐에 다 들어가 있고, "라운드"는 그중 고정된 15개 세트가 아니라 **"연속으로 몇 번
성공(조금앎/완벽함)했는지"를 세는 진행률 개념**입니다(`doneCount % 15 === 0`일 때
라운드 완료 화면). 모름/헷갈림 단어는 큐에서 빠지지 않고 다시 꽂히지만 큐 전체가
항상 살아있으므로, 재등장하기 전에 항상 다른(서로 다른) 단어들이 여러 개 끼어듭니다
— "라운드에 단어 하나만 남아서 계속 반복"되는 상황 자체가 구조적으로 생기지 않습니다.
그래서 "새 단어 잠금"이나 "모르는 단어 5개 이하면 새 단어 투입" 같은 추가 장치는
필요 없다고 판단해 구현하지 않았고, 라운드 크기 15 / 의미(15개 맞히기) / 모름·헷갈림
재등장 규칙은 현재 상태 그대로 확정합니다.
