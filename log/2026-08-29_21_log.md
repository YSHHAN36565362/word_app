# 2026-08-29 — 마스코트 배너, SRS 다듬기, 저작권 정리

## 요청/배경
- 연습 모드 등에서 irasutoya.com(저작권 프리) 이미지를 활용해 정답/오답 반응을
  화면에 크게 넣어달라는 요청으로 시작.
- 그 뒤로 이어서: 깃허브 기여자 표시 제거, 안키(Anki) 기능 참고, 간격 반복을 날짜
  기준으로 개선할 수 있는지 등 여러 요청이 이어짐.

## 한 일
- **마스코트 반응 배너**: 정답/오답 시 보여주는 캐릭터를 irasutoya.com 일러스트로
  교체.
  - 1차: 화면 폭을 꽉 채우는 큰 배너(`object-cover`)로 구현 → "이미지가 잘리고
    너무 크다"는 피드백을 받아 화면 절반 크기 · `object-contain`(안 잘리고 전체가
    보이도록) 방식으로 재수정. TV/HDMI 연결처럼 해상도가 달라져도 안 잘리도록
    상대 단위 + 최대 크기 캡 적용.
  - 오답(모름/헷갈림) 반응 이미지를 1장 → 9장으로 확장(유머러스한 이미지 포함): 한숨
    쉬는 사람, 해고당한 회사원, 벼 베는 농부, 재수생, 백기 든 사람, 이불 뒤집어쓴
    사람, 로댕의 생각하는 사람, 폭포 수행 스님, 사막을 걷는 사람. `reactionKey`를
    해시해서 결정론적으로 고르는 방식으로 구현(React 렌더 순수성 규칙 때문에 렌더
    중 `Math.random()`을 바로 못 씀).
- **SRS 우선순위 개선**: 연습 큐 정렬 시, 아직 복습일이 안 된 "잘 아는 단어"는 한
  번 더 뒤로 미루는 로직(`NOT_DUE_PUSH`)을 `prioritizeByMastery`에 추가.
- **Git 히스토리 정리**: 커밋 작성자(author)는 원래부터 전부 사용자 본인 계정이었고,
  실제로는 9개 커밋 메시지에만 "Co-authored-by: Claude" 트레일러가 남아있던 것을
  발견 → `git filter-branch`로 그 줄만 제거하고 force-push(실행은 사용자가 직접
  GitHub Desktop으로). 이후 GitHub의 Contributors 표시가 한동안 안 지워진 건 GitHub
  쪽 캐시가 늦게 갱신되는 문제로 확인(코드/데이터 자체는 이미 깨끗함).
- **PageHeader 아이콘 시안**: 각 파트(학습/연습/매칭게임/시험/오답노트/설정)의 글자
  배지를 일러스트로 바꾸는 안을 `test/page-header-preview.html`에 목업으로 제작
  (검토용, 아직 실제 코드에는 반영 안 함).
- **안키(Anki) 덱 조사**: `JLPT-MAX-Deck-2.0.3.apkg`(1.07GB, 어휘 9,160개 + 오디오
  24,978개)를 직접 열어봤으나, 뜻풀이 근거가 특정 상업 교재(해커스 등)로 표시돼
  있어 저작권 우려로 그대로 가져다 쓰지 않기로 결정. `.gitignore`에 `*.apkg` 추가
  (실수로 커밋되는 사고 방지).
- **JMdict/WordNet 기반 단어 목록 생성**: Jisho API(JMdict, CC BY-SA 3.0)로 읽기·뜻·
  JLPT 태그를 검증하며 N1 단어 80개(3개 파일, 기존 프롬프트 형식 그대로 적용) 작성.
  Japanese WordNet(NICT, 조건부 자유 라이선스)은 "이 단어가 실존하는지" 확인하는
  보조 자료로 일부 사용.
- **`word_list_pure` 폴더 신설**: JMdict의 `jlpt-n1`/`jlpt-n2` 태그가 붙은 단어
  전체를 가져온 뒤, 다중 급수 태그(예: N5 단어에도 N1 태그가 같이 붙은 오염 사례)·
  가타카나 외래어·숫자기호·대명사 등을 걸러내 N1 2,700→798개, N2 1,393→335개로
  정제. "JMdict 태그 데이터 자체가 옛 구(舊)급수 기준의 불완전한 스팟체크 목록"이라는
  한계도 함께 문서화(사용자가 예시로 든 躊躇·彷徨う 등 표준 단어 일부가 애초에 이
  데이터셋에 없다는 것도 확인).
