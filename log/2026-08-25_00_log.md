# 2026-08-25 ~ 08-28 (대략) — 핵심 기능 대량 추가

이 구간은 대화가 한 번 요약(압축)된 뒤라 정확한 날짜/시간 구분이 어렵습니다. 실제로는
며칠에 걸쳐 순차적으로 요청·구현된 내용입니다.

## 한 일
- **간격 반복(SRS) 시스템 도입**: `word_mastery` 테이블에 `wrong_count`/
  `interval_days`/`repetition`/`next_review_at` 컬럼 추가. `computeNextMastery`로
  채점 점수에 따라 다음 복습일을 계산하고, "오늘의 복습" 카드로 복습 화면에 노출.
- **오프라인 대비 localStorage 보조 저장**: 네트워크가 잠깐 끊겨도 이 기기에 즉시
  보조 저장해뒀다가, 연결이 복구되면 서버와 자동으로 병합.
- **마이크로 러닝**: 연습을 15개 단위 "라운드"로 나누고, 라운드를 채울 때마다 짧은
  완료 화면을 보여줌.
- **진행률 바를 "젤리" 스타일 애니메이션**으로 교체.
- **매칭 게임 애니메이션 전면 개편**: 타일을 맞췄을 때/틀렸을 때의 색상·움직임 효과.
- **힌트 텍스트의 대괄호 헤더**(`[읽기]`, `[치트키]` 등) 하이라이트 처리.
- **기본 테마를 라이트 → 다크로 변경**, UI 전반에서 이모지 제거.
- **하단 탭 내비게이션에 매칭 게임 탭 추가** 및 탭 순서 재정렬.
- **단어 발음을 들을 수 있는 TTS(SpeakButton)** 추가 — 브라우저 내장 음성 합성만
  사용, 외부 API 없음.
- **플로팅 메모장(MemoPad) 추가**: 학습/연습 화면 위에 손글씨로 메모할 수 있는 창.
  드래그로 이동, 모서리로 크기 조절, Apple Pencil 전용 필기(손가락 터치 무시로
  아이패드에서 손바닥이 닿아도 안 그려짐) 등 여러 차례 다듬음. "이동 가능하다는 걸
  한눈에 알아보게 해달라"는 요청에 따라 6점 그립 아이콘 + 헤더 배경색으로 보완.
